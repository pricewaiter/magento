#!/bin/bash

echo "Setting permissions for Magento Connect (this can take a little while)"

cd /var/www/htdocs

find . -type d -exec chmod 700 {} \;
find . -type f -exec chmod 600 {} \;
chown -R www-data:www-data .

echo "Done. Run reset-magento-permissions to reset permissions."
